import {
  textPreview
} from '../../../helpers/text-preview';
import { module, test } from 'qunit';

module('TextPreviewHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = textPreview(42);
  assert.ok(result);
});
