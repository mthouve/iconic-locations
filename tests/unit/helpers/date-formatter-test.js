import {
  dateFormatter
} from '../../../helpers/date-formatter';
import { module, test } from 'qunit';

module('DateFormatterHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateFormatter(42);
  assert.ok(result);
});
